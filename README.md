# Vue loader
A simple loader for Vue.js applications

## Usage
```vue
<template>
  <Loader>Loading...</Loader>
</template>

<script>
import Loader from '@moreillon/Loader.vue'

export default {
  name: 'my_component',
  components: { Loader }
}
</script>
```
